Templates in Flask are HTML files used to create dynamic web pages. Flask uses Jinja2, a powerful templating engine, to embed Python logic in these templates. 
Static files like CSS, JavaScript and images are stored separately to enhance the user interface. 
This section covers how to use templates and static files effectively in Flask.